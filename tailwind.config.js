module.exports = {
    content: [
      './templates/**/*.html.twig',  // Inclure les fichiers Twig
      './assets/**/*.js',            // Inclure les fichiers JS (si utilisé)
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };